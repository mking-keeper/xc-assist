#!/bin/sh

# Rebuild xc-assist if TypeScript files changed
if git diff --cached --name-only | grep -q "mcp-servers/xc-assist/src/"; then
  echo "📦 Building xc-assist..."
  cd mcp-servers/xc-assist
  npm run build
  cd ../..
  git add mcp-servers/xc-assist/dist/
fi
